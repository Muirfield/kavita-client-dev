[pytest]
testpaths = tests
markers =
    integration: requires a running Kavita docker container
    release: core endpoints (auth, invite) that gate publishing a release
    compat: broad endpoint sweep used for regression tracking
